{
  "skill_name": "cmux",
  "evals": [
    {
      "id": 1,
      "prompt": "Split the terminal and open a second pane on the right with zsh.",
      "expected_output": "Agent uses cmux new-pane --type terminal to create a new terminal pane, not new-split from Claude Code context.",
      "files": [],
      "assertions": [
        {
          "name": "uses-new-pane-not-new-split",
          "description": "Response uses 'cmux new-pane --type terminal' rather than 'cmux new-split' which fails from Claude Code sessions"
        },
        {
          "name": "extracts-surface-id",
          "description": "Response captures the returned surface:N ID for subsequent commands"
        },
        {
          "name": "uses-send-for-text-send-key-for-special",
          "description": "Response uses 'cmux send' for text input and 'cmux send-key' only for special keys like Return, ctrl+c"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "Type 'hello world' into the pane on surface:42 and press enter.",
      "expected_output": "Agent uses cmux send for the text and cmux send-key for Return, not send-key for individual characters.",
      "files": [],
      "assertions": [
        {
          "name": "send-for-text-not-send-key",
          "description": "Response uses 'cmux send --surface surface:42 \"hello world\"' not individual send-key calls for letters"
        },
        {
          "name": "send-key-for-return",
          "description": "Response uses 'cmux send-key --surface surface:42 Return' for the enter key"
        },
        {
          "name": "correct-surface-targeting",
          "description": "Response targets surface:42 explicitly in both commands"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "Just use cmux send-key to type each character of 'npm test' and hit enter. It's simpler that way.",
      "expected_output": "Agent corrects the approach — send-key fails for letter characters. Uses cmux send for text instead.",
      "files": [],
      "assertions": [
        {
          "name": "rejects-send-key-for-text",
          "description": "Response explains that send-key fails for letter characters and recommends cmux send instead"
        },
        {
          "name": "does-not-use-individual-character-send-key",
          "description": "Response does not attempt to send individual characters via send-key"
        }
      ]
    }
  ]
}
