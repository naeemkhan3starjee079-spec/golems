#!/usr/bin/env zsh
#
# Ralph Pre-Commit Hook
# Runs syntax checks, linting, and custom pattern analysis before each commit.
#
# Install: git config core.hooksPath .githooks
# Or run: ./scripts/setup-hooks.sh
#

set -e

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo "${BLUE}  🔍 Ralph Pre-Commit Checks${NC}"
echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

ERRORS=0
WARNINGS=0

# Get list of staged .zsh files
STAGED_ZSH_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.zsh$' || true)
REPO_ROOT=$(git rev-parse --show-toplevel)

# ═══════════════════════════════════════════════════════════════
# 1-5. ZSH FILE CHECKS (only when .zsh files are staged)
# ═══════════════════════════════════════════════════════════════
if [[ -z "$STAGED_ZSH_FILES" ]]; then
  echo "${GREEN}✓ No .zsh files staged, skipping syntax/lint checks (1-5)${NC}"
  echo ""
else
  echo "Checking files: $STAGED_ZSH_FILES"
  echo ""

  # ═══════════════════════════════════════════════════════════════
  # 1. ZSH SYNTAX CHECK
  # ═══════════════════════════════════════════════════════════════
  echo "${YELLOW}[1/8] ZSH Syntax Check${NC}"

  for file in ${(f)STAGED_ZSH_FILES}; do
    if zsh -n "$file" 2>&1; then
      echo "  ${GREEN}✓${NC} $file"
    else
      echo "  ${RED}✗${NC} $file has syntax errors"
      ERRORS=$((ERRORS + 1))
    fi
  done
  echo ""

  # ═══════════════════════════════════════════════════════════════
  # 2. SHELLCHECK (for .sh files only - doesn't support zsh)
  # ═══════════════════════════════════════════════════════════════
  echo "${YELLOW}[2/8] ShellCheck Linting${NC}"

  # ShellCheck doesn't support zsh (SC1071), so we only run it on .sh files
  # The zsh syntax check in step 1 is sufficient for .zsh files

  # Get staged .sh files
  STAGED_SH_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.sh$' || true)

  if [[ -z "$STAGED_SH_FILES" ]]; then
    echo "  ${GREEN}✓${NC} No .sh files staged"
    echo "  Note: .zsh files use zsh -n for syntax checking (step 1)"
  elif ! command -v shellcheck &> /dev/null; then
    echo "  ${YELLOW}⚠${NC} shellcheck not installed - skipping .sh file linting"
    echo "  Install with: brew install shellcheck"
    WARNINGS=$((WARNINGS + 1))
  else
    for file in ${(f)STAGED_SH_FILES}; do
      # Run shellcheck with common exclusions
      # SC1091: Not following sourced file (can't resolve paths)
      # SC2034: Variable appears unused (common in sourced files)
      # SC2088: Tilde in quotes warning (often intentional for display labels)
      if shellcheck -e SC1091,SC2034,SC2088 "$file" 2>&1; then
        echo "  ${GREEN}✓${NC} $file"
      else
        echo "  ${RED}✗${NC} $file has shellcheck warnings/errors"
        ERRORS=$((ERRORS + 1))
      fi
    done
  fi
  echo ""

  # ═══════════════════════════════════════════════════════════════
  # 3. CUSTOM PATTERN CHECKS (Bug Prevention)
  # ═══════════════════════════════════════════════════════════════
  echo "${YELLOW}[3/8] Custom Bug Pattern Checks${NC}"

  for file in ${(f)STAGED_ZSH_FILES}; do
    FILE_ERRORS=0

    # Check 1: break/continue outside of loop control structures
    # break/continue should be inside while, for, or until loops
    # We check for loop keywords in the 30 lines before break to allow for nested blocks
    if grep -n 'break$' "$file" | while read line; do
      linenum=$(echo "$line" | cut -d: -f1)
      # Get context: 30 lines before (loops can have nested if/case blocks)
      start_line=$((linenum - 30))
      [[ $start_line -lt 1 ]] && start_line=1
      context=$(sed -n "${start_line},${linenum}p" "$file")
      # Check if break is inside a loop (while/for/until)
      # Note: 'done' closes loops, so break with no preceding loop keyword is suspicious
      if ! echo "$context" | grep -qE '\b(while|for|until)\b'; then
        echo "  ${RED}✗${NC} $file:$linenum - 'break' may be outside loop (no while/for/until in context)"
        FILE_ERRORS=$((FILE_ERRORS + 1))
      fi
    done; [[ $FILE_ERRORS -gt 0 ]]; then
      ERRORS=$((ERRORS + FILE_ERRORS))
    fi

    # Check 2: Using $? after a command that might overwrite it
    if grep -nE '\|\s*tee.*\n.*\$\?' "$file" 2>/dev/null | head -1 | grep -q .; then
      echo "  ${YELLOW}⚠${NC} $file - Using \$? after pipe may capture wrong exit code (use \${pipestatus[1]})"
      WARNINGS=$((WARNINGS + 1))
    fi

    # Check 3: Unquoted variable expansions in conditionals
    # Note: In zsh [[ ]], unquoted variables are safe (no word splitting/globbing)
    # Only flag string comparisons with = or ==, not numeric -eq/-lt/-gt
    if grep -nE '\[\[.*\$[a-zA-Z_][a-zA-Z0-9_]*\s*(=|==)\s*[^"$]' "$file" 2>/dev/null | head -3 | grep -q .; then
      echo "  ${YELLOW}⚠${NC} $file - Unquoted variable in string comparison (consider quoting)"
      WARNINGS=$((WARNINGS + 1))
    fi

    # Check 4: eval usage (potential security issue)
    # Only flag actual eval commands, not comments mentioning eval
    if grep -n '^\s*eval\b\|;\s*eval\b' "$file" 2>/dev/null | head -1 | grep -q .; then
      echo "  ${YELLOW}⚠${NC} $file - Uses 'eval' - consider using array expansion instead"
      WARNINGS=$((WARNINGS + 1))
    fi

    # Check 5: Hardcoded sleep values > 30s (might indicate forgotten debug code)
    if grep -nE 'sleep\s+[3-9][0-9]|sleep\s+[0-9]{3,}' "$file" 2>/dev/null | head -1 | grep -q .; then
      echo "  ${YELLOW}⚠${NC} $file - Long sleep detected (>30s) - is this intentional?"
      WARNINGS=$((WARNINGS + 1))
    fi

    # Check 6: TODO/FIXME/HACK/XXX comments
    if grep -niE '(TODO|FIXME|HACK|XXX):?' "$file" 2>/dev/null | head -3 | grep -q .; then
      echo "  ${YELLOW}⚠${NC} $file - Contains TODO/FIXME comments (non-blocking)"
      WARNINGS=$((WARNINGS + 1))
    fi

    if [[ $FILE_ERRORS -eq 0 ]]; then
      echo "  ${GREEN}✓${NC} $file - no critical patterns found"
    fi
  done
  echo ""

  # ═══════════════════════════════════════════════════════════════
  # 4. RETRY LOGIC INTEGRITY CHECK
  # ═══════════════════════════════════════════════════════════════
  echo "${YELLOW}[4/8] Retry Logic Integrity${NC}"

  for file in ${(f)STAGED_ZSH_FILES}; do
    # Check that retry loops have proper structure
    if grep -q 'while.*retry' "$file" 2>/dev/null; then
      # Ensure there's a continue in the retry logic
      if ! grep -qE 'continue\s*$' "$file" 2>/dev/null; then
        echo "  ${RED}✗${NC} $file - Retry loop found but no 'continue' statement"
        ERRORS=$((ERRORS + 1))
      fi

      # Ensure max_retries is defined
      if ! grep -q 'max_retries' "$file" 2>/dev/null; then
        echo "  ${RED}✗${NC} $file - Retry loop found but no 'max_retries' defined"
        ERRORS=$((ERRORS + 1))
      fi

      # Ensure retry_count is incremented
      if ! grep -qE 'retry_count=.*\+' "$file" 2>/dev/null; then
        echo "  ${RED}✗${NC} $file - Retry loop found but retry_count not incremented"
        ERRORS=$((ERRORS + 1))
      fi

      if [[ $ERRORS -eq 0 ]]; then
        echo "  ${GREEN}✓${NC} $file - retry logic structure looks correct"
      fi
    else
      echo "  ${GREEN}✓${NC} $file - no retry logic to check"
    fi
  done
  echo ""

  # ═══════════════════════════════════════════════════════════════
  # 5. FUNCTION BALANCE CHECK
  # ═══════════════════════════════════════════════════════════════
  echo "${YELLOW}[5/8] Brace/Bracket Balance${NC}"

  for file in ${(f)STAGED_ZSH_FILES}; do
    # Count opening and closing braces (important for function definitions)
    # Note: This counts ALL braces including those in ${...} variable expansions
    # and strings, so we tolerate small imbalances (up to 10)
    OPEN_BRACES=$(grep -o '{' "$file" | wc -l | tr -d ' ')
    CLOSE_BRACES=$(grep -o '}' "$file" | wc -l | tr -d ' ')
    BRACE_DIFF=$((OPEN_BRACES - CLOSE_BRACES))
    BRACE_DIFF=${BRACE_DIFF#-}  # absolute value

    if [[ "$BRACE_DIFF" -gt 10 ]]; then
      # Only error on significant imbalance (>10) - smaller differences are likely
      # from ${var} expansions, heredocs, or string literals
      echo "  ${RED}✗${NC} $file - Large brace imbalance: { = $OPEN_BRACES, } = $CLOSE_BRACES (diff: $BRACE_DIFF)"
      ERRORS=$((ERRORS + 1))
    elif [[ "$BRACE_DIFF" -gt 0 ]]; then
      # Small imbalance - warn but don't block (likely ${var} expansions)
      echo "  ${YELLOW}⚠${NC} $file - Minor brace imbalance: { = $OPEN_BRACES, } = $CLOSE_BRACES (diff: $BRACE_DIFF, likely \${var} syntax)"
      WARNINGS=$((WARNINGS + 1))
    else
      echo "  ${GREEN}✓${NC} $file - braces balanced"
    fi

    # Parentheses often appear in regex/strings - warn only, don't block
    OPEN_PARENS=$(grep -o '(' "$file" | wc -l | tr -d ' ')
    CLOSE_PARENS=$(grep -o ')' "$file" | wc -l | tr -d ' ')
    PAREN_DIFF=$((OPEN_PARENS - CLOSE_PARENS))
    PAREN_DIFF=${PAREN_DIFF#-}  # absolute value

    if [[ "$PAREN_DIFF" -gt 5 ]]; then
      # Only warn if difference is significant (>5 indicates likely real issue)
      echo "  ${YELLOW}⚠${NC} $file - Parentheses imbalance: ( = $OPEN_PARENS, ) = $CLOSE_PARENS (may be regex/strings)"
      WARNINGS=$((WARNINGS + 1))
    fi
  done
  echo ""
fi

# ═══════════════════════════════════════════════════════════════
# 6. JSON SCHEMA VALIDATION (always runs)
# ═══════════════════════════════════════════════════════════════
echo "${YELLOW}[6/8] JSON Schema Validation${NC}"

# Check for staged JSON files in schemas/
STAGED_JSON_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.json$' || true)

if [[ -z "$STAGED_JSON_FILES" ]]; then
  echo "  ${GREEN}✓${NC} No JSON files staged"
else
  for file in ${(f)STAGED_JSON_FILES}; do
    if jq '.' "$file" > /dev/null 2>&1; then
      echo "  ${GREEN}✓${NC} $file - valid JSON"
    else
      echo "  ${RED}✗${NC} $file - invalid JSON syntax"
      ERRORS=$((ERRORS + 1))
    fi
  done
fi
echo ""

# ═══════════════════════════════════════════════════════════════
# 7. TEST SUITE
# ═══════════════════════════════════════════════════════════════
echo "${YELLOW}[7/8] Test Suite${NC}"

# Run ralph tests
TEST_SCRIPT=""
if [[ -f "$REPO_ROOT/tests/test-ralph.zsh" ]]; then
  TEST_SCRIPT="$REPO_ROOT/tests/test-ralph.zsh"
elif [[ -f "$HOME/.config/ralphtools/tests/test-ralph.zsh" ]]; then
  TEST_SCRIPT="$HOME/.config/ralphtools/tests/test-ralph.zsh"
fi

if [[ -n "$TEST_SCRIPT" && -x "$TEST_SCRIPT" ]]; then
  echo "  Running ralph tests..."
  echo ""

  # Capture test output and exit code
  TEST_OUTPUT=$("$TEST_SCRIPT" 2>&1) || TEST_EXIT_CODE=$?
  TEST_EXIT_CODE=${TEST_EXIT_CODE:-0}

  # Show output (tests have their own formatting)
  echo "$TEST_OUTPUT"

  if [[ $TEST_EXIT_CODE -ne 0 ]]; then
    echo ""
    echo "  ${RED}✗ Ralph tests failed${NC}"
    ERRORS=$((ERRORS + 1))
  else
    echo "  ${GREEN}✓${NC} Ralph tests passed"
  fi
else
  echo "  ${YELLOW}⚠${NC} test-ralph.zsh not found - skipping ralph tests"
  WARNINGS=$((WARNINGS + 1))
fi

# Run skills tests
SKILLS_TEST_SCRIPT=""
if [[ -f "$REPO_ROOT/tests/test-skills.zsh" ]]; then
  SKILLS_TEST_SCRIPT="$REPO_ROOT/tests/test-skills.zsh"
fi

if [[ -n "$SKILLS_TEST_SCRIPT" && -x "$SKILLS_TEST_SCRIPT" ]]; then
  echo ""
  echo "  Running skills tests..."
  echo ""

  # Capture test output and exit code
  SKILLS_TEST_OUTPUT=$("$SKILLS_TEST_SCRIPT" 2>&1) || SKILLS_TEST_EXIT_CODE=$?
  SKILLS_TEST_EXIT_CODE=${SKILLS_TEST_EXIT_CODE:-0}

  # Show output (tests have their own formatting)
  echo "$SKILLS_TEST_OUTPUT"

  if [[ $SKILLS_TEST_EXIT_CODE -ne 0 ]]; then
    echo ""
    echo "  ${RED}✗${NC} Skills tests failed"
    ERRORS=$((ERRORS + 1))
  else
    echo "  ${GREEN}✓${NC} Skills tests passed"
  fi
else
  echo "  ${YELLOW}⚠${NC} test-skills.zsh not found - skipping skills tests"
fi

# Run Bun tests (if bun/ directory exists)
if [[ -d "$REPO_ROOT/bun" ]]; then
  echo ""
  echo "  Running Bun tests..."
  echo ""

  # Capture test output and exit code
  BUN_TEST_OUTPUT=$(cd "$REPO_ROOT/bun" && bun test 2>&1) || BUN_TEST_EXIT_CODE=$?
  BUN_TEST_EXIT_CODE=${BUN_TEST_EXIT_CODE:-0}

  # Show output (tests have their own formatting)
  echo "$BUN_TEST_OUTPUT"

  if [[ $BUN_TEST_EXIT_CODE -ne 0 ]]; then
    echo ""
    echo "  ${RED}✗ Bun tests failed${NC}"
    ERRORS=$((ERRORS + 1))
  else
    echo "  ${GREEN}✓${NC} Bun tests passed"
  fi
else
  echo "  ${GREEN}✓${NC} No bun/ directory - skipping Bun tests"
fi
echo ""

# ═══════════════════════════════════════════════════════════════
# 8. AGENTS.MD SYNC (if modified)
# ═══════════════════════════════════════════════════════════════
echo "${YELLOW}[8/8] AGENTS.md Sync${NC}"

# Check if AGENTS.md is staged for this commit
if git diff --cached --name-only | grep -q '^AGENTS\.md$'; then
  # Find sync-agents.sh (check common locations)
  SYNC_SCRIPT=""

  if [[ -f "$HOME/.config/ralphtools/scripts/sync-agents.sh" ]]; then
    SYNC_SCRIPT="$HOME/.config/ralphtools/scripts/sync-agents.sh"
  elif [[ -f "$REPO_ROOT/scripts/sync-agents.sh" ]]; then
    SYNC_SCRIPT="$REPO_ROOT/scripts/sync-agents.sh"
  fi

  if [[ -n "$SYNC_SCRIPT" && -x "$SYNC_SCRIPT" ]]; then
    echo "  ${GREEN}✓${NC} AGENTS.md modified - syncing to AI tool files..."
    "$SYNC_SCRIPT"
  else
    echo "  ${YELLOW}⚠${NC} sync-agents.sh not found - skipping AGENTS.md sync"
    WARNINGS=$((WARNINGS + 1))
  fi
else
  echo "  ${GREEN}✓${NC} AGENTS.md not modified - no sync needed"
fi
echo ""

# ═══════════════════════════════════════════════════════════════
# SUMMARY
# ═══════════════════════════════════════════════════════════════
echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"

if [[ $ERRORS -gt 0 ]]; then
  echo "${RED}  ✗ COMMIT BLOCKED: $ERRORS error(s) found${NC}"
  echo "${YELLOW}  ⚠ $WARNINGS warning(s) (non-blocking)${NC}"
  echo ""
  echo "  Fix the errors above before committing."
  echo "  To bypass (not recommended): git commit --no-verify"
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  exit 1
elif [[ $WARNINGS -gt 0 ]]; then
  echo "${GREEN}  ✓ COMMIT ALLOWED (with $WARNINGS warning(s))${NC}"
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  exit 0
else
  echo "${GREEN}  ✓ ALL CHECKS PASSED${NC}"
  echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
  exit 0
fi
